var limite_cal;
limite_cal = (limite - 25)
if instance_exists(obj_flagpole)
{
    if (limite_cal <= (obj_flagpole.y + 17))
        limite_final = (obj_flagpole.y + 17)
    else
        limite_final = limite_cal
}

