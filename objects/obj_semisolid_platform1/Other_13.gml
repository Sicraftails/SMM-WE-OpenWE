with (instance_create(x, y, obj_modelsizable))
{
    sprite = other.sprite_index
    size_x = other.size_x
    size_y = other.size_y
    depth = other.depth
    image_xscale = other.size_x
    type = 0
}
