if (hspeed != 0)
{
    image_speed = (0.2 * increment)
    alarm[11] = 30
    if ((!instance_exists(obj_mario)) || obj_mario.x < x)
        hspeed = (-1.2 * increment)
    else
        hspeed = (1.2 * increment)
}
else
    alarm[11] = 5

